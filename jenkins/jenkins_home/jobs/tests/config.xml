<?xml version='1.0' encoding='UTF-8'?>
<project>
  <actions/>
  <description></description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <org.jenkinsci.plugins.gitlablogo.GitlabLogoProperty plugin="gitlab-logo@1.0.1">
      <repositoryName></repositoryName>
    </org.jenkinsci.plugins.gitlablogo.GitlabLogoProperty>
  </properties>
  <scm class="hudson.plugins.git.GitSCM" plugin="git@2.4.0">
    <configVersion>2</configVersion>
    <userRemoteConfigs>
      <hudson.plugins.git.UserRemoteConfig>
        <url>https://github.com/sfl-drupal/indus-demo</url>
      </hudson.plugins.git.UserRemoteConfig>
    </userRemoteConfigs>
    <branches>
      <hudson.plugins.git.BranchSpec>
        <name>master</name>
      </hudson.plugins.git.BranchSpec>
    </branches>
    <doGenerateSubmoduleConfigurations>false</doGenerateSubmoduleConfigurations>
    <submoduleCfg class="list"/>
    <extensions/>
  </scm>
  <assignedNode>aegir</assignedNode>
  <canRoam>false</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenDownstreamBuilding>false</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  <triggers/>
  <concurrentBuild>false</concurrentBuild>
  <builders>
    <hudson.tasks.Shell>
      <command>#HASH=${GIT_COMMIT:0:4}
PLATEFORM=my-7x-${HASH}-${BUILD_NUMBER}

# This is our candidate artifact for deployment
if ! test -e $WORKSPACE/build; then
  mkdir $WORKSPACE/build
else
  rm -rf $WORKSPACE/build/*
fi
cd $WORKSPACE/src/drupal
# Compress the thing
tar -czf $WORKSPACE/build/demo-7.x-${BUILD_NUMBER}.tar.gz .

exit 0

# we can has verbose
set -x
# abort if error
set -e

if ! test -e $WORKSPACE/build; then
  mkdir $WORKSPACE/build
else 
  rm -rf $WORKSPACE/build/*.tar.gz
fi


cd $WORKSPACE
echo &quot;Reading Jenkins database config....&quot; &gt;&amp;2
if [ -r $WORKSPACE/.drush-deploy.rc ]; then
  echo &quot;Reading database config....&quot; &gt;&amp;2
  . $WORKSPACE/.drush-deploy.rc
else
  echo &quot;Database credentials not found&quot;;
  exit 1;
fi

# Clean up first
if test -e $WORKSPACE/src/drupal; then
  if test -e $WORKSPACE/src/drupal/sites/default; then
     chmod +w $WORKSPACE/src/drupal/sites/default
  fi
  sudo rm -rf $WORKSPACE/src/drupal
fi

# Clone or update the profil
if test -e $WORKSPACE/reblochon; then
  rm -rf $WORKSPACE/reblochon
fi
git clone git@github.com:philippemouchel/reblochon.git

# Bootstrap nouveau, utilise le cache ~jenkins/.drush/cache/download
#drush make --translations=fr $WORKSPACE/reblochon/build/fromages.make src/drupal --contrib-destination=profiles/reblochon
drush make $WORKSPACE/build/fromages.make src/drupal --contrib-destination=profiles/reblochon

#######################################################################################################################################

# This is our candidate artifact for deployment
if ! test -e $WORKSPACE/build; then
  mkdir $WORKSPACE/build
else
  rm -rf $WORKSPACE/build/*
fi
cd $WORKSPACE/src/drupal
# Compress the thing
tar -czf $WORKSPACE/build/$PLATEFORM.tar.gz .
# And add a permanent link to it
# cd $WORKSPACE/build
# ln -s $PLATEFORM.tar.gz $PLATEFORM_PERMANENT.tar.gz

##########################################################################################################

# Uncomment the following line to force a successful build
# exit 0

# Prepare test behat
cd $WORKSPACE
cp $WORKSPACE/tests/behat/example.behat.yml $WORKSPACE/tests/behat/behat.yml
sed -i &quot;s@%DRUPAL_ROOT@`pwd`/src/drupal@g&quot; $WORKSPACE/tests/behat/behat.yml
sed -i &quot;s@%URL@http://${LOCALHOSTNAME}@g&quot; $WORKSPACE/tests/behat/behat.yml

# We need to create an empty DB for drupal using our test user account
mysqladmin -f -u${DB_USER} -p${DB_PASS} drop ${DB_NAME}
mysqladmin -u${DB_USER} -p${DB_PASS} create ${DB_NAME}

cd $WORKSPACE/src/drupal
drush si --yes  --db-url=&quot;mysql://$DB_USER:$DB_PASS@localhost/$DB_NAME&quot; --site-name=&quot;Drupal Camp MTL 2015&quot; reblochon --account-name=admin --account-pass=admin --clean-url=1
chmod a+w $WORKSPACE/src/drupal/sites/default/files

# Enable Espace Étudiant
drush en epoisses -y

# Revert features
drush fra -y

# Clear cache
drush cc all

drush status

# Test section
#mkdir -p ${WORKSPACE}/logs/behat
#cd ${WORKSPACE}/tests/behat
#bin/behat --format pretty --tags &quot;~@wip&amp;&amp;~@disabled&amp;&amp;~@local&quot; --colors</command>
    </hudson.tasks.Shell>
  </builders>
  <publishers>
    <hudson.tasks.ArtifactArchiver>
      <artifacts>build/*.tar.gz</artifacts>
      <allowEmptyArchive>false</allowEmptyArchive>
      <onlyIfSuccessful>false</onlyIfSuccessful>
      <fingerprint>false</fingerprint>
      <defaultExcludes>true</defaultExcludes>
    </hudson.tasks.ArtifactArchiver>
  </publishers>
  <buildWrappers>
    <hudson.plugins.ansicolor.AnsiColorBuildWrapper plugin="ansicolor@0.4.1">
      <colorMapName>xterm</colorMapName>
    </hudson.plugins.ansicolor.AnsiColorBuildWrapper>
  </buildWrappers>
</project>